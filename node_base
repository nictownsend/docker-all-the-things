#!/bin/sh

docker run -i -p 3000:3000 -p 8080:8080 -p 443:443 -p 8443:8443 -v "$(pwd)":/workdir -v node_modules:/usr/npm_global -v "${HOME}"/.npmrc:/root/.npmrc -v "${HOME}"/.aws:/root/.aws -w /workdir node:14 sh -c "export PATH=$PATH:/usr/npm_global/bin; ${*}"
